import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  container: {
    alignItems: "center",
  },
  dolar: {
    fontSize: 20,
    fontWeight: "bold",
  },
});

export default style;
